name: Déploiement GitHub Pages et actualisation quotidienne

on:
  push:
    branches: [main]
  schedule:
    # Tous les jours à 06:15 UTC (le guide Cardmarket est régénéré vers 02:45, heure de Paris).
    - cron: '15 6 * * *'
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

env:
  CARDTRADER_TOKEN: ${{ secrets.CARDTRADER_TOKEN }}

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Vérifier l'application et la sauvegarde
        run: npm test && npm run backup:check
      - name: Actualiser les correspondances du catalogue Cardmarket
        if: ${{ github.event_name != 'push' }}
        run: node scripts/update-prices.mjs
      - name: Mettre à jour les prix des annonces VF (CardTrader)
        if: ${{ github.event_name != 'push' && env.CARDTRADER_TOKEN != '' }}
        run: node scripts/update-prices-fr.mjs
        env:
          CARDTRADER_TOKEN: ${{ secrets.CARDTRADER_TOKEN }}
      - name: Enregistrer les prix dans le dépôt
        if: ${{ github.event_name != 'push' }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add public/data/prices.json public/data/history.json
          [ -f public/data/prices-fr.json ] && git add public/data/prices-fr.json
          git diff --cached --quiet || git commit -m "data: prix CardTrader FR et catalogue du $(date -u +%Y-%m-%d)"
          git push
      - name: Construire l'application
        run: npm run build
        env:
          BASE_PATH: /${{ github.event.repository.name }}/
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist
  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
